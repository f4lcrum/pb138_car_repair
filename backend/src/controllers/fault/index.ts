import createFault from './create';
import updateFault from './update';
import readFault from './read';
import assign from './assign';
import readAllFaults from './readAll';

export default {
  createFault, updateFault, readFault, assign, readAllFaults,
};
