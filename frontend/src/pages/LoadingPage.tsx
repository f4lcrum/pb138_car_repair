import { Typography } from "@mui/material";
import { FC } from "react";

const LoadingPage: FC = () => {
  return (
    <Typography variant="h3" color="primary">
      Loading gears, tires and tools
    </Typography>
  );
};

export default LoadingPage;
